---
title: "3.3 assert"
date: 2019-08-30T15:13:01+08:00
weight: 03030000
---

```c++
#include <assert.h>
#include <stdio.h>

int main() {
    int a;
    char str[50];

    printf("请输入一个整数值： ");
    scanf("%d\n", &a);

    assert(a >= 10);
    printf("输入的整数是：%d\n", a);

    printf("请输入字符串： ");
    scanf("%s\n", &str);
    assert(str != NULL);
    printf("输入的字符串是： %s\n", str);

    return 0;
}


a.out: assert.c:15: main: Assertion `a >= 10' failed.
已放弃 (核心已转储)

```
